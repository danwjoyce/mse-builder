# Tools Tab : Configuration / Settings for application

Tools.tab.UI <- function() {
  fluidRow(
    box(title = "Global Configuration", status = "primary", width = 6 ),
    box(title = "Information", status = "primary", width = 6 )
  )
}